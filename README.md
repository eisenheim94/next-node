# Issue Tracker Learning Workspace

## Services
- Frontend: Next.js app in `apps/frontend`
- Backend: NestJS app in `apps/backend`
- PostgreSQL: `localhost:5433`
- RabbitMQ: `localhost:5672`
- RabbitMQ management UI: `http://localhost:15672`
- Swagger docs: `http://localhost:3001/api/docs`

## Quick start
1. Copy `.env.example` to `.env`.
2. Run `npm install` in the repo root.
3. Run `docker compose up -d`.
4. Run `npm run dev:backend`.
5. Run `npm run dev:frontend`.

## Learning roadmap
The detailed roadmap lives in `LEARNING_PLAN.md`.
